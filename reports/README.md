# Reports

This folder holds the paper writeup(s) for *The Peacekeepers' Arms Race*, as distinct from the analysis notebooks in `notebooks/` and the raw output in `figures/` and `tables/`.

Planned contents:

- `draft.md` (or `.docx`/`.pdf`) — the full journal-submission-track manuscript: stranger-audience framing, expanded literature review, methods, results, discussion.
- `replication_package/` — a self-contained bundle for reviewers (pinned environment, minimal script to regenerate headline figures/tables from `data/clean/`).
- Author statements / supplementary materials, once the target journal is decided.

Nothing is here yet — this file exists so the folder is tracked by git (which doesn't track empty directories) ahead of the actual writeup landing.
